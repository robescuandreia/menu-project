# menu-project

## Use of Map in jsx to add a list of components

```
<div>
    {array.map((el) => (<span>{el}</span>))}
</div>
```

## Use of Map in javascript

```
array.map(el => {
    newEl = el + el
    return newEl
})
```

```
array.map(el => el)
```
